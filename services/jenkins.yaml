---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: jenkins
  name: jenkins
spec:
  ports:
    - port: 80
      protocol: TCP
      targetPort: 8080
  selector:
    app: jenkins
  type: LoadBalancer
